services:
  chrome-homepage:
    image: nginx:alpine
    container_name: chrome-homepage
    ports:
      - "4000:80"
    volumes:
      - ./dist/index.html:/usr/share/nginx/html/index.html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
